namespace FPTJobMatch.Models
{
    public enum JobStatus
    {
        Pending = 1,
        Approved = 2,
        Rejected = 3
    }

    public enum CandidateStatus
    {
        Pending = 1,
        Approved = 2,
        Rejected = 3
    }
}
